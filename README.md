# amthor.dehna.it

Dieses Projekt liegt live unter `/srv/apps/amthor`.

Deployment-Ziel:

- Working tree: `/srv/apps/amthor`
- Bare repository: `/srv/git/amthor.git`

Der Live-Stack wird per `docker compose` aus `compose.yml` gestartet.
